'use strict';

/**
 * Adds commas to a number
 * @param {number} number
 * @param {string} locale
 * @return {string}
 */
module.exports = function(name) {
    console.log("Hello world, Nice to meet you " + name + " :D");
};